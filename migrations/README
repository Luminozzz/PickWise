Single-database configuration for Flask.

Put a proper file directory first
export FLASK_APP=(database.models):create_app
(database.models) -> change to your specified file

!!! Before creating a new version, change the settings of your table in your models file. !!!

Creating a new version of table
flask db migrate -m "title"

Check for any errors in the versions folder file.

If no error
flask db upgrade