# Dev only needs the proxy target (where FastAPI runs). Defaults to localhost:8000.
VITE_API_TARGET=http://localhost:8000

# For a production build served from a different origin than the API, set the
# full backend origin here instead (used by src/api.js).
# VITE_API_URL=https://api.pickwise.example.com